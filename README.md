# lib2class

## 使用方法

1. 克隆本仓库

```bash
$ git clone https://github.com/FoliageOwO/lib2class.git
```

2. 安装依赖

```bash
$ pip install -r requirements.txt
```

3. 将题库文件放进 `docs` 文件夹内
4. 执行生成脚本

```bash
$ python generator.py
```

5. 生成结果在根目录的 `(文档名).json`
